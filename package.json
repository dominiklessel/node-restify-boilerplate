{
  "name": "node-restify-boilerplate",
  "version": "1.0.1",
  "private": true,
  "scripts": {
    "start": "node app",
    "test": "NODE_ENV=production mocha --reporter spec test/*.test.js"
  },
  "dependencies": {
    "restify": "2.8.x",
    "nconf": "0.6.x",
    "bunyan": "0.23.x",
    "request": "2.37.x",
    "lodash": "*"
  },
  "devDependencies": {
    "supertest": "^0.13.0"
  }
}
