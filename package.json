{
  "name": "node-restify-boilerplate",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "start": "node app",
    "test": "NODE_ENV=production mocha --reporter spec test/*.test.js"
  },
  "dependencies": {
    "acl": "0.4.x",
    "bunyan": "1.0.x",
    "lodash": "*",
    "nconf": "0.6.x",
    "request": "2.42.x",
    "restify": "2.8.x"
  },
  "devDependencies": {
    "supertest": "^0.13.0"
  }
}
